<%= form_for @order do |f| %>

<%= f.label :party_id %>
<%= f.select( :party_id, options_for_select(@parties.map {|party| [party.reservation_name, party.id ]}) ) %>

<%= f.label :item %>
<%= f.select( :item_id, options_for_select(@item.map {|item| [item.name, item.id ]}) ) %>
<%= f.submit %>

<% end %>
<%= link_to "DELETE", order_path(@order), method: :delete %>
